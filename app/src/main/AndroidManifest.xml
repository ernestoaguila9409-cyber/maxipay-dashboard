<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label=""
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">

        <!-- Login Screen (Launcher) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- POS Screens -->
        <activity android:name=".MainActivity" android:exported="false" />
        <activity android:name=".MenuActivity" android:exported="false" />
        <activity android:name=".MenuOnlyActivity" android:exported="false" />
        <activity android:name=".PaymentActivity" android:exported="false" />

        <!-- Employees -->
        <activity android:name=".EmployeesActivity" android:exported="false" />

        <!-- Batch -->
        <activity android:name=".BatchManagementActivity" android:exported="false" />
        <activity android:name=".BatchDetailsActivity" android:exported="false" />

        <!-- Transactions -->
        <activity android:name=".TransactionActivity" android:exported="false" />
        <activity android:name=".ModifierManagementActivity" />
        <activity android:name=".GlobalModifierActivity"/>
        <activity android:name=".AssignModifierToItemActivity"/>
    </application>

</manifest>