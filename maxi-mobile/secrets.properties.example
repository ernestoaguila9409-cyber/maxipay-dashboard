# Copy to secrets.properties (gitignored). No quotes around values.
SPIN_TPN=
SPIN_REGISTER_ID=
SPIN_AUTH_KEY=
# Production SPIn base (override for UAT if your ISO provides a different host)
SPIN_BASE_URL=https://spinpos.net/v2
