"use client";

import Header from "@/components/Header";

/** Empty shell for report routes; navigation-only until real pages are built. */
export default function ReportSectionPlaceholder({ title }: { title: string }) {
  return <Header title={title} />;
}
